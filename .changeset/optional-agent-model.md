---
"@yogioo/sandcastle": minor
---

Make agent factory model arguments optional (`pi()`, `claudeCode()`, `codex()`, …) so omitted models use the CLI default instead of a hardcoded Sandcastle string.
