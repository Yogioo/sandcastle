---
"@yogioo/sandcastle": patch
---

Register a module resolve hook when `sandcastle` launches a generated runner so `@yogioo/sandcastle` is loaded from the CLI installation instead of the target repository.
