# Handoff: Sandcastle planning workflow

**Repo:** `C:\projects\sandcastle`  
**Branch:** `main`  
**Date:** 2026-08-13  
**Next session focus:** implement `sandcastle plan` — do not re-open the product decisions.

This file is a session handoff. Delete it after the work is picked up.

## Status

Design is locked. Implementation has **not** started except glossary in `CONTEXT.md`.

The last assistant turn asked the user to reply **开干** to confirm TDD seams. The user left for the evening instead. Treat the four seams below as **approved** and start the red-green loop. Do not re-grill Q1–Q6 unless they reopen them.

## Locked product decisions (all A)

Two sibling processes, same issue tracker, different list filters:

| Command | Entry | Sees |
| --- | --- | --- |
| `sandcastle` / `sandcastle .` | `main.mts` / `main.ts` | **ready tasks** only |
| `sandcastle plan` / `sandcastle plan .` | `plan.mts` / `plan.ts` | **discussion tasks** only |

Flow for a discussion task:

1. Human opens a requirements-discussion issue (`needs-planning`).
2. Host probe: start an agent only when there is work (never grilled, or latest comment is human, or a phase label is waiting). Waiting on a human → idle poll, **no sandbox**.
3. Three separate `run()` iterations, never one session invoking two skills:
   - **grill** → comments questions on the issue (async grilling; ticket *is* the session)
   - frontier empty → label `aligned` → next loop iteration **immediately** runs **spec** (no extra human “确认”, no idle)
   - spec posted as a comment on the parent → `specced` → next iteration **immediately** runs **tickets**
4. Tickets creates child **ready tasks**. Parent stays open as epic, label `planned`, comment with child links. Implement loop never lists the parent.

Why spec and tickets are separate: context collision / phase-skipping. Isolation belongs in the **orchestrator** (one phase per `run()`), not in “please call two skills in order”.

IDE skills (`grill-me`, `to-spec`, `to-tickets`) are the **behavior spec**. The AFK loop does **not** invoke `/grill-me` inside the sandbox (`disable-model-invocation`). Write `grill-prompt.md` / `spec-prompt.md` / `tickets-prompt.md` that speak the same protocols over issue comments.

## Glossary (do not paraphrase — read it)

`CONTEXT.md` — terms added this session:

- **Implement workflow** vs **Planning workflow**
- **Discussion task** vs **Ready task**
- **Planning phase** (grill | spec | tickets)
- **Iteration** broadened (planning may write tracker, no commit)

**Name collision:** `recipes/planner/` is plan→parallel execute→merge for *implementation*. Never call the new loop “planner”. CLI is `plan`. Files: `plan.mts`, not `plan-prompt.md` (that name is already the parallel-planner recipe prompt).

## Architecture notes (not yet in an ADR)

- **Not** a third init template. Lives inside **standard** as a sibling entry (`plan.mts` next to `main.mts`). Matches ADR 0030: `main.ts` stays a single implement path; no feature-flag matrix.
- **Not** a workflow recipe you apply later — user wants `sandcastle plan` first-class after init.
- **blank** does not get a planning entry. Missing `plan.*` → clear CLI error; do not fall back to implement.
- Template self-containment: ADR 0009. Follow the existing `logs.mts` pattern: extract probe helpers next to `plan.mts`, test them from `src/` (see `src/standard-template-logs.test.ts`) so they are not copied as a Sandcastle public API.
- `rewriteMainTs` currently only rewrites `main.ts`/`main.mts`. Planning entry needs the same agent-factory + sandbox-provider rewrite, and `main.mts`→`main.ts` style rename for `plan.mts`→`plan.ts`.
- Project manifest stays one `entryFile` (`main.*`). Convention: sibling `plan.*` with the same extension. Do not bump `project.json` schema unless forced.

### Tracker split

**GitHub:** implement list unchanged (`--label Sandcastle`). Parent never gets `Sandcastle`. Planning list: `--label needs-planning` excluding `planned`.

**Beads:** today’s implement list is bare `bd ready --json`, which **would pick up** an unblocked discussion issue. New scaffolds must use `bd ready --exclude-label needs-planning --json`. Planning list: `bd list --label needs-planning` (exclude `planned`). Comments: `bd comments` / `bd comments add`.

**Custom:** extend `SETUP_ISSUE_TRACKER.md` with planning list / comment / label / create markers.

**Human-vs-agent comments (host probe):** do not require GitHub author identity. Agent comments start with a fixed marker (same idea as triage’s “generated by AI” disclaimer). Latest comment lacks the marker → human replied → `grill`.

### Label state machine

```
needs-planning
  → aligned     (grill done)
  → specced     (spec comment posted)
  → planned     (children created; parent stays open)
```

One loop iteration = one phase `run()`. After `aligned`, the next `while` iteration sees spec work and must **not** idle-sleep.

## TDD seams (test only these)

1. **CLI `sandcastle plan [path]`** — same project resolution as `sandcastle [path]`; spawns `plan.mts`/`plan.ts`; missing entry → explicit error; never starts implement.
2. **Init (standard)** — copies planning entry + three prompts; blank has no planning entry; beads implement `LIST_TASKS_COMMAND` gains `--exclude-label needs-planning`; GitHub implement list still `Sandcastle` only.
3. **Planning host probe** — input labels+comments → `grill` | `spec` | `tickets` | `wait`. Rules:
   - never grilled, or latest comment is human → `grill`
   - `aligned` and not `specced` → `spec`
   - `specced` and not `planned` → `tickets`
   - latest comment is agent and not `aligned` → `wait` (no sandbox)
   - `planned` → absent from the list
4. **Phase isolation** — one `run()` per loop iteration; `aligned` then spec then tickets with no idle between them.

Do **not** test grill question text, spec prose, or ticket granularity (prompt quality, not orchestrator).

Start with seam 1 (CLI). Vertical slices after that: init copy/rewrite → probe helper → `plan.mts` loop → three prompts → tracker template args (GitHub first, then beads/custom).

## Suggested first files

Read before coding:

- `CONTEXT.md` (glossary)
- `docs/adr/0009-templates-no-shared-code.md`
- `docs/adr/0030-standard-and-blank-workflow-templates.md`
- `src/cli.ts` (`rootCommand`, `runRegisteredProject`, `spawnProjectRunner`)
- `src/ProjectRunner.ts`
- `src/InitService.ts` (`IssueTrackerEntry`, `scaffold`, `rewriteMainTs`, `copyTemplateFiles`)
- `src/templates/standard/main.mts` (idle-poll loop to copy, not to merge into)
- `src/cli.test.ts`, `src/InitService.test.ts`, `src/standard-template-logs.test.ts`
- `docs/agents/adding-an-issue-tracker.md` if expanding tracker commands
- Behavior specs (do not run as IDE skills in the sandbox):
  - `C:\Users\EDY\.agents\skills\grilling\SKILL.md`
  - `C:\Users\EDY\.agents\skills\to-spec\SKILL.md`
  - `C:\Users\EDY\.agents\skills\to-tickets\SKILL.md`

Likely new/changed:

- `src/cli.ts` — `plan` subcommand
- `src/cli.test.ts`
- `src/InitService.ts` — extra tracker args, rewrite `plan.*`, beads exclude-label
- `src/templates/standard/plan.mts`
- `src/templates/standard/grill-prompt.md`, `spec-prompt.md`, `tickets-prompt.md`
- probe helpers beside them (name TBD, same pattern as `logs.mts`)
- `src/templates/standard/AGENTS.md`
- `src/templates/standard/recipes/issue-tracker/README.md`
- optional ADR under `docs/adr/` (CLI split + issue-as-session; hard to reverse, surprising vs planner recipe)

New tracker template args will probably include `LIST_PLANNING_TASKS_COMMAND` plus comment/label/create commands. Implement `LIST`/`VIEW`/`CLOSE` stay; planning must not close the parent.

GitHub label creation: today init only creates `Sandcastle` when `createLabel` is true. Planning labels (`needs-planning`, `aligned`, `specced`, `planned`) need a story (create at init vs agent `gh label create`). Do not block seam 1 on this.

## Out of scope

- Changing the implement loop’s behavior except beads list exclusion on **new** scaffolds
- Cursor Automations
- Merging planning into the same process as implement
- A second human gate between spec and tickets

## Suggested skills

1. **tdd** — red before green; only the four seams above.
2. **codebase-design** — keep `plan.mts` a thin host loop; probe logic behind a small helper interface (same leverage as `logs.mts`).
3. **domain-modeling** — write an ADR when the CLI/entry/label protocol is coded; do not invent new glossary synonyms.
4. **code-review** — after a vertical slice, Standards + Spec against this handoff and `CONTEXT.md`.

Do **not** start with grill-me / plan-maker / plan-coordinator. Product shape is done.

## Resume prompt (paste into the next chat)

```
Continue the Sandcastle planning workflow from HANDOFF-planning-workflow.md
and CONTEXT.md. Decisions are locked. Start TDD at seam 1:
`sandcastle plan [path]` spawns plan.mts, not main.mts.
```
